{
  "name": "plantuml-document-server",
  "version": "1.0.0",
  "description": "A document server that allows collaboration on PlantUML diagrams.",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:bot\"",
    "dev:frontend": "cd frontend && npm run start",
    "dev:backend": "cd backend && npm run start",
    "format": "prettier --write ."
  },
  "dependencies": {
    "concurrently": "^9.0.1",
    "npm": "^10.7.0",
    "prettier": "^3.3.2"
  }
}
